/**
 * Giriş/kayıt üst şeridi — `SiteHeader` `h-[5.5rem]` ile aynı olmalı.
 * AuthFullscreenLayout: marginTop / paddingTop negatif çekiş için kullanılır.
 */
export const AUTH_HEADER_HEIGHT_REM = "5.5rem";
